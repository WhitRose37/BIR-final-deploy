{
  "version": 2,
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm ci && npx prisma generate",
  "framework": "nextjs",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
